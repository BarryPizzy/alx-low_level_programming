#include "main.h"
#include <string.h>

/**
 * _strcmp - a function to compare string
 * @s1: first param
 * @s2: second param
 * Return: a compared string
 */

int _strcmp(char *s1, char *s2)
{
	int result

	result = strcmp(s1, s2);
	return (result);
}
