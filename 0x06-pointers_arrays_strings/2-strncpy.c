#include "main.h"
#include <string.h>

/**
 * *_strncpy - a function to copy string
 * @dest: first param
 * @src: secong param
 * @n: third param
 * Return: a character and a string
 */

char *_strncpy(char *dest, char *src, int n)
{
	_strncpy(dest, src, n);
	return (dest);
}
