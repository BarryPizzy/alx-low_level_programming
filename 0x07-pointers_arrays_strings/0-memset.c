#include "main.h"

/**
 * _memset - a function to fill memory with constant bytes
 * @s: a string
 * @b: a character
 * @n: an integer
 *
 * Return: a string
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s,b.n))
}
